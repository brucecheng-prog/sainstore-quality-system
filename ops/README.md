# QMS 运维检查

运行 qms_healthcheck.py 可只读检查开发者、局域网和外网三个约定入口。

统一入口必须返回 healthz JSON，并确认 Cookie 签名密钥及照片令牌已配置。此检查不读取或修改报告、图片、NAS 文件及数据库业务数据。

发布前后均应执行一次。若局域网或外网未返回 JSON 健康状态，表示该入口仍运行旧版直接 Streamlit 链或网络路由没有指向当前统一入口，不应宣告发布完成。
